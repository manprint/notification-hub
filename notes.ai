## Gestione utenze

Al momento la creazione delle utenze richiede l'invio delle mail.
Voglio che sia possibile creare un utenza direttamente dal pannello di admin (sezione owner) specificado direttamente la mail e la password che l'utente utilizzerà per loggarsi.


- utenza
- gruppi di receiver

- possibilità di associare un utenza a uno o più gruppi di receiver
- un utenza deve avete la CRUD per creare/modificare/cancellare nuovi receiver (a seconda del suo grado di membership)
  - owner: crud completa su tutti i possibili gruppi
  - admin: crud completa su tutti i possibili gruppi
  - member: crud completa sui gruppi a cui è associato
  - viever: solo visualizzazione delle modifiche sui gruppi a cui è associato

## Gestione utenti/gruppi di receiver.

Voglio che per ogni utente possa associare uno o più gruppi di receiver che deve poter gestire.
Devo poter specificare uno o più gruppi al momento della creazione dell'utente o dopo tramite modifica dell'utente.
Rispetto ai gruppi, è necessario rispettare le seguenti gerarchie di CRUD:
- un utenza deve avere la CRUD per creare/modificare/cancellare nuovi receiver (a seconda del suo grado di membership)
    - owner: crud completa su tutti i possibili gruppi
    - admin: crud completa su tutti i possibili gruppi
    - member: crud completa sui gruppi a cui è associato
    - viever: solo visualizzazione delle modifiche sui gruppi a cui è associato

## Opus REVIEW
Ho bigogno che mi fai una review completa dell'applicazione alla ricerca di bug.
I primi che ho notato io sono:
- Se ad un utenza rimuovo un gruppo con owner/admin, quell'utenza continua a vedere il gruppo.
- La sezione consegne non capisco cosa faccia e come funzionale
- La pagina dei canali è sformattata e in alcune sezioni trasborda dai limiti delle box definite.
- La sezione override in canali non funziona (internal server error)

Guardati prima questi e poi fammi una hard review di tutto.
Correggi quello che è baggato.

*********************
claude --resume fdab023a-b88b-4e8b-9eda-0c01414afc02
*********************

Voglio che mi realizzi per questo progetto un Dockerfile.all.
Tutti i file necessari alla creazione dei container unico devono andare in deploy/all-in-one/; in questa cartella crea anche l'env di example ed il compose all-in-one, nonchè un readme per utilizzare l'immagine.
Questo Dockerfile deve costruire un immagine completa, autosufficiente, con tutti i componenti del docker compose di produzione. Usa, se possibile, debian 13 come base image.
L'obiettivo è avviare un solo container che mi rende l'app disponibile.
Per l'orchiestrazione dei servizi all'interno dei container puoi usare supervisord o s3-init (scegli tu il più efficiente ed il più stabile)
IL container deve avere un healtcheck che verifica che tutti i servizi sono up e l'applicazione compelta è up and running.
Il container deve contenere tutto: minio, postgres, redis, etc. e tutta la parte applicativa.
Il container deve gestire gli aggiornamenti in modo pulito: passare da 1.0.0 a 1.1.0 p 2.0.0 non deve creare incosistenze o errori sui dati. Quindi il container deve occuparsi di migrazioni, aggiornamento dati, etc.
L'immagine deve prevedere una sola cartella di persistenza completa per i dati e le configurazioni "/data" che io monto e persiste tutto in sottocartelle. La monetrò come volume.
L'immagine deve avere poi una sola cartella per i log dei servizi "/logs" ce monetrò e ha le sottocartelle per ogni servizio con i log.
Facendo docker logs -f (stdout) voglio vedere tutti i log in streaming nella forma "[service] - riga di log (la riga classica di log con data, etc)"
La build del container unico deve essere fatta tramite il makefile in progetto.




Gestione errori ricorrenti (ad esempio in arco temporale di minuti configurabile)
Monitotaggio di durata di esecuzione dello script.
Gestione dell'escalation.
Integrazione con ITOP.


***************************************
***************************************

2. Bug preesistente incontrato durante la prova, fuori dal tuo sviluppo: NOTIFYHUB_BOOTSTRAP_EMAIL con dominio special-use (.local, .test, .example) viene accettato dal bootstrap CLI ma rifiutato al login con 422 da email-validator — l'owner creato non può entrare. Dimmi se lo sistemo (validazione in bootstrap o allineamento fra i due), non l'ho toccato.

Idea per dopo, non ora: ping di inizio esecuzione dal wrapper (X-Phase: start), per distinguere "cron non è partito" da "job partito alle 3 e mai finito perché la macchina è caduta a metà backup". Con i soli ping di fine i due casi sono indistinguibili — ma entrambi fanno scattare l'allarme, quindi la v1 non ne ha bisogno.